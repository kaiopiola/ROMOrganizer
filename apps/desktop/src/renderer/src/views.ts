/**
 * Telas do app.
 *
 * `library` é a biblioteca aberta; as outras são visões sobre a coleção inteira e por isso
 * não dependem de haver uma pasta selecionada.
 */
export type View = 'library' | 'playlists' | 'queue' | 'settings'
